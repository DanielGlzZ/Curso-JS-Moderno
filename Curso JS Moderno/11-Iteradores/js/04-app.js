let i = 1; //Incializar el While

while( i < 100 ) { //Condición

    if(i % 2 === 0) {
        console.log(`El número es par ${i}`);
    } else {
        console.log(`El número es impar ${i}`);
    }

    i++; //Incremento
}