const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true
}

// Esta parte nos muestra las llaves de un objeto
console.log( Object.keys( producto ) );

// Esta parte nos muestra los valores de un objeto
console.log( Object.values( producto ) );

// Esta parte nos muestra todo lo de un objeto
console.log(Object.entries( producto ));